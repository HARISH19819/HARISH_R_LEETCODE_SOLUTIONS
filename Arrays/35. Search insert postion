class Solution:
    def searchInsert(self, nums: List[int], target: int) -> int:
        if target in nums:
            return nums.index(target)
        else:
            target-=1
            while target not in nums and target>nums[0]:
                target-=1
            if target not in nums:
                return 0
            return nums.index(target)+1
